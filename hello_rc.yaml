apiVersion: v1
kind: ReplicationController
metadata:
  name: hello
  labels:
    name: hello
spec:
  replicas: 2
  selector:
    name: hello
  template:
    metadata:
      labels:
        name: hello
    spec:
      containers:
      - name: hello
        image: hello
        ports:
        - containerPort: 8080
